import 'package:flutter/material.dart';

class SearchVolesPage extends StatelessWidget {
  const SearchVolesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text("")),
    );
  }
}
